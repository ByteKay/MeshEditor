1 click MeshEditor.exe to open the window

2 it will show you a folder on the left of window, you can double-click it.
  Then it will expand and you will see many files with two post suffix which are "hxs" and "hxa".
	Note: the hxs format is the model with skin, and the other is the animation of the model

3 you can double-click the file with .hxs, then the model will show in the main window of QT

4 there are so many useful functions that are sopportted to optimise the model

5 Of course, you can download the app version on the app store with the key word "mesh editor".
  the window's version is not maintained for a long time.

Enjoy it
